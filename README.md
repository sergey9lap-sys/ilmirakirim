# Сайт Ильмиры Кирим

Самостоятельная статическая копия актуальной страницы из `personal-world/sites/ilmira`, перенесённая 23 сентября 2026 года. Прежняя версия по `/p/ilmira` остаётся в `myworld` и не изменена. Репозитории не синхронизируются автоматически.

## Размещение

`index.html` находится в корне: для показа сайта достаточно направить веб-сервер на каталог репозитория. Сборка и Node.js на сервере не нужны. Все используемые файлы — `assets/`, `documents/`, `vendor/`, `style.css`, `client-feedback.css` и `app.js` — уже включены.

Для будущих отдельных страниц можно добавлять каталоги вида `имя-страницы/index.html`, сохраняя главную страницу в корне.

## Яндекс.Метрика

Счётчик `113009416` подключён в `app.js` с настройками, выданными Метрикой. Он запускается только на `ilmirakirim.ru` и `www.ilmirakirim.ru` после нажатия «Согласиться» в уведомлении о cookie; закрытие уведомления не запускает аналитику. На локальных адресах счётчик отключён, чтобы тестовые визиты не попадали в отчёты. Поля имени, почты и телефона помечены `ym-disable-keys` для Вебвизора.

`noscript`-пиксель из стандартного кода сознательно не установлен: без JavaScript сайт не может получить и проверить выбор посетителя по cookie-уведомлению. Если механизм согласия изменится, пересмотреть этот пункт отдельно. После публикации открыть `https://ilmirakirim.ru/?_ym_debug=2`, нажать «Согласиться» и проверить номер счётчика и PageView в панели отладки. Автоматическая проверка без нажатия на cookie-уведомление может не обнаружить отложенный счётчик.

## Домен

- В canonical, Open Graph, структурированных данных, `robots.txt` и `sitemap.xml` указан согласованный адрес `https://ilmirakirim.ru`.
- Перед открытием сайта проверить настройки DNS, HTTPS и работу всех ссылок, форм и документов на новом сервере. Изменение репозитория само по себе не размещает сайт на хостинге.
- Копирование в этот репозиторий не обновляет прежнюю страницу автоматически; дальнейшие правки для нового домена вносить сюда отдельно.
